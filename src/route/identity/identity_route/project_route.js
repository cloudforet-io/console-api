import express from 'express';
import projectService from '../identity_service/project_service';

const projectRoute = express.Router();

// this must] be declared at end of the file.
export default projectRoute;
