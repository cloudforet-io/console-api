import express from 'express';
import apiKeyService from '../IdentityService/apiKeyService';

const apiKeyRoute = express.Router();

// this must] be declared at end of the file.
export default apiKeyRoute;
