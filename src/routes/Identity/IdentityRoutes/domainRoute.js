import express from 'express';
import domainService from '../IdentityService/domainService';

const domainRoute = express.Router();

// this must] be declared at end of the file.
export default domainRoute;
